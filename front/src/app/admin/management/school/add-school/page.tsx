'use client';

import { Select } from 'antd';
import SchoolForm from '@/app/components/AddSchool';

const { Option } = Select;
const page = () => {
    return (
        <>
            <SchoolForm />
        </>
    );
}

export default page;